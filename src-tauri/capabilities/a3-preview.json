{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "a3-preview",
  "description": "Capability for the pop-out A3 preview window (2026-08-04) — least-privilege: this window never opens dialogs, never creates other windows, only receives descriptor pushes from the main window.",
  "windows": ["a3-preview"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:event:allow-emit"
  ]
}
