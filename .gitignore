# --- OS ---
.DS_Store
._*
Thumbs.db

# --- Local-only reference material -------------------------------------------
# Five completed company A3s (~110 MB of .xls, mostly embedded screenshots).
# Analysed 2026-08-01; the conclusions live in docs/02_REAL_WORLD_FINDINGS.md.
# Deliberately NOT tracked: git never forgets a blob, so committing these would
# bloat every future clone permanently. See reference/Examples/README.md.
reference/Examples/

# --- Node / Vite (Phase 0) ---
node_modules/
dist/
dist-ssr/
*.local
.vite/

# --- Rust / Tauri (Phase 0) ---
src-tauri/target/
src-tauri/gen/

# Faz 8 Dilim 3 (D-20): build.rs stages this from capabilities-e2e/ only when
# the `e2e-test` Cargo feature is active, and removes it otherwise on every
# build — self-healing, but ignored too in case a build is killed mid-way.
src-tauri/capabilities/e2e-test.json

# --- Tooling ---
.venv/
venv/
__pycache__/
coverage/
playwright-report/
test-results/
*.log
*.profraw

# Office gecici kilit dosyalari (dosya Excel/Word ile acikken olusur)
~$*
